[Unit]
Description=Cloudflare Tunnel - TMPoly
After=network-online.target
Wants=network-online.target

[Service]
Type=notify
ExecStart=/usr/bin/cloudflared --no-autoupdate tunnel run --token-file /etc/cloudflared/tmpoly-token
Restart=on-failure
RestartSec=5s

[Install]
WantedBy=multi-user.target
